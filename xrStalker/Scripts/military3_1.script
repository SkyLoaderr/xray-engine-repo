-- скрипт для сталкеров, которые стоят на вышках

class "Guardian"

function Guardian:__init (npc_object, script_name)
    self.npc = npc_object
    self.script_name = script_name
	self.position = npc:position ()
end

function Guardian:start ()
    self.npc:script (true, self.script_name)
end


function Guardian:stop ()
  self.npc:script (false, self.script_name)
end


function main ()

	local guard1 = Guardian (get_level_object ("stalker_tower1"), military3_1.script_name ())
	local guard2 = Guardian (get_level_object ("stalker_tower2"), military3_1.script_name ())

	guard1:start ()
	guard2:start ()

--	action (stalker1, move (move.standing, move.stand, move.line, patrol ("simple_way")), 
--			look (look.search), anim (anim.free), cond (cond.time_end, 1000000))
--	action (stalker2, move (move.standing, move.stand, move.line, patrol ("simple_way")), 
--			look (look.search), anim (anim.free), cond (cond.time_end, 1000000))

	while true do wait () end

	guard1:stop ()
	guard2:stop ()
--	stalker1:script (false, military3_1.script_name ())
--	stalker2:script (false, military3_1.script_name ())

end