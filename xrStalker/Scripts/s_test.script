function main ()


	local stalker = get_level_object ("m_stalker_e0000")
	stalker:script (true, s_test.script_name ())
--	local torch = get_level_object ("torch")
--	if (torch ~= nil) then printf ("Getting torch") flush () end

--	action (stalker, move (move.standing, move.walk, move.line, patrol ("way0000")),
--			look (look.search), anim (anim.free), object (torch, object.activate), 
--			cond (cond.time_end, 1000000000000))

--	while stalker:action () do 
--		  if stalker:alive () == false then break end
--		  wait () 
--		  end

--	stalker:script (false, s_test.script_name ())

	local door = get_level_object ("door")
	local bones = {"bone16", "bone22"}

	local starthit = 10;
	while true do
		  wait (500)
		  local h = hit ()	
          h.power = 1
          h.direction:set (-1, 0, 0)
          h:bone (bones[math.random (1, table.getn (bones))])
          h.draftsman = get_actor ()
          h.impulse = starthit
          h.type  = hit.strike
          door:hit (h)
		  starthit = starthit + math.random (100, 200)
	end			

 end