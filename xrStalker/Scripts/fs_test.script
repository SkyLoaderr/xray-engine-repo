
function main()
	local f = getFS()
	local flist = f:file_list_open("$screenshots$",FS.FS_ListFiles)
	local f_cnt = flist:Size()
	
	for	it=0, f_cnt-1 	do
		local file		=	flist:GetAt(it)
		printf("file[%d]=%s",it,file)
   end
	
	flist:Free()
end
