
function aes_segeant_kill (stalker, player)
    local h = hit ()
    h.power = 100
    h.direction = vector():set (0, -1, 0)
    h.impulse = 1
    h.draftsman = player
    h.type = hit.strike
    stalker:hit (h)
end
