-----------------------------------------------------------------------
-- —ÓÒÚÓˇÌËˇ ·‡Á˚ ·‡Ì‰ËÚÓ‚
--    0 - ÌÓÏ‡Î¸ÌÓÂ ÒÓÒÚÓˇÌËÂ
--    2 - ÚÂ‚Ó„‡
-----------------------------------------------------------------------

local state_normal = 0
local state_alarm = 1
local state_alarm_actor_inside = 2
local state_fight = 1

function load_job(sj, gname, type, squad, groups)
    ------Stalkers-------------------------------------------------------
	if      type == "val_lager_bandits" then    load_job_lager_bandits  (sj, gname, type, squad, groups)
	elseif  type == "val_monolith_base" then    load_job_monolith_base  (sj, gname, type, squad, groups)
	elseif  type == "val_war_soldiers"  then    load_job_war_soldiers   (sj, gname, type, squad, groups)
    ------Monsters-------------------------------------------------------
    --- Dogs
	elseif  type == "val_dogs_nest1"    then    load_job_dogs_nest1     (sj, gname, type, squad, groups)
	elseif  type == "val_dogs_nest2"    then    load_job_dogs_nest2     (sj, gname, type, squad, groups)
	elseif  type == "val_dogs_nest3"    then    load_job_dogs_nest3     (sj, gname, type, squad, groups)
	elseif  type == "val_dogs_nest4"    then    load_job_dogs_nest4     (sj, gname, type, squad, groups)
	elseif  type == "val_dogs_nest5"    then    load_job_dogs_nest5     (sj, gname, type, squad, groups)
	--- Fleshes
	elseif  type == "val_fleshes_nest1" then    load_job_fleshes_nest1  (sj, gname, type, squad, groups)
	elseif  type == "val_fleshes_nest2" then    load_job_fleshes_nest2  (sj, gname, type, squad, groups)
	elseif  type == "val_fleshes_nest3" then    load_job_fleshes_nest3  (sj, gname, type, squad, groups)
	elseif  type == "val_fleshes_nest4" then    load_job_fleshes_nest4  (sj, gname, type, squad, groups)
	elseif  type == "val_fleshes_nest5" then    load_job_fleshes_nest5  (sj, gname, type, squad, groups)
	--- Boars
	elseif  type == "val_boars_nest1"   then    load_job_boars_nest1    (sj, gname, type, squad, groups)
	elseif  type == "val_boars_nest2"   then    load_job_boars_nest2    (sj, gname, type, squad, groups)
	elseif  type == "val_boars_nest3"   then    load_job_boars_nest3    (sj, gname, type, squad, groups)
	elseif  type == "val_boars_nest4"   then    load_job_boars_nest4    (sj, gname, type, squad, groups)
	elseif  type == "val_boars_nest5"   then    load_job_boars_nest5    (sj, gname, type, squad, groups)
	end
end

function lb_predicate1(obj) 
	return obj:profile_name() == "bandit_borov"
end

function lb_predicate2(obj) 
	return obj:profile_name() == "bandit_veteran"
end

function lb_predicate3(obj) 
	return obj:profile_name() == "bandit_general"
end

function load_job_lager_bandits(sj, gname, type, squad, groups)
	-----------------------------------------------------------------------
	-- œ–»Œ–»“≈“ 9 - ¡Œ–Œ¬
	-----------------------------------------------------------------------
	t = { section = "logic@val_lager_bandits_leader_normal",
		idle = 0,
		prior = 9, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_borov_restrictor",
		predicate = lb_predicate1
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_borov"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_leader_alarm",
		idle = 0,
		prior = 9, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_borov_restrictor",
		predicate = lb_predicate1
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_borov"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_leader_alarm",
		idle = 0,
		prior = 9, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_borov_restrictor",
		predicate = lb_predicate1
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_borov"
		            --end
	}
	table.insert(sj, t)

	-----------------------------------------------------------------------
	-- œ–»Œ–»“≈“ 8 - Œ’–¿ÕÕ» » ¡Œ–Œ¬¿
	-----------------------------------------------------------------------
	t = { section = "logic@val_lager_bandits_bodyguard_normal1",
		idle = 0,
		prior = 8, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_main_building_restrictor",
		predicate = lb_predicate2
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_veteran"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_bodyguard_alarm1",
		idle = 0,
		prior = 8, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_main_building_restrictor",
		predicate = lb_predicate2
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_veteran"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_bodyguard_alarm1",
		idle = 0,
		prior = 8, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_main_building_restrictor",
		predicate = lb_predicate2
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_veteran"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_bodyguard_normal2",
		idle = 0,
		prior = 8, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_bodyguards_restrictor",
		predicate = lb_predicate2
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_veteran"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_bodyguard_alarm2",
		idle = 0,
		prior = 8, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_bodyguards_restrictor",
		predicate = lb_predicate2
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_veteran"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_bodyguard_alarm2",
		idle = 0,
		prior = 8, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_bodyguards_restrictor",
		predicate = lb_predicate2
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_veteran"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_bodyguard_normal3",
		idle = 0,
		prior = 8, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_bodyguards_restrictor",
		predicate = lb_predicate2
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_veteran"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_bodyguard_alarm3",
		idle = 0,
		prior = 8, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_bodyguards_restrictor",
		predicate = lb_predicate2
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_veteran"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_bodyguard_alarm3",
		idle = 0,
		prior = 8, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_bodyguards_restrictor",
		predicate = lb_predicate2
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_veteran"
		            --end
	}
	table.insert(sj, t)

	-----------------------------------------------------------------------
	-- œ–»Œ–»“≈“ 7 - —»ƒﬂŸ»≈ ¬  ≈Ãœ≈ 1 ¬Œ«À≈ ¡Œ–Œ¬¿ » ¬  ≈Ãœ≈ 2 Õ¿ ¬“Œ–ŒÃ ›“¿∆≈
	-----------------------------------------------------------------------
	t = { section = "logic@val_lager_bandits_kamp1_normal1",
		idle = 0,
		prior = 7, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_main_building_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp1_alarm1",
		idle = 0,
		prior = 7, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_main_building_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp1_alarm1",
		idle = 0,
		prior = 7, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_main_building_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp1_normal2",
		idle = 0,
		prior = 7, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_main_building_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp1_alarm2",
		idle = 0,
		prior = 7, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_main_building_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp1_alarm2",
		idle = 0,
		prior = 7, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_main_building_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp1_normal3",
		idle = 0,
		prior = 7, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_main_building_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp1_alarm3",
		idle = 0,
		prior = 7, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_main_building_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp1_alarm3",
		idle = 0,
		prior = 7, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_main_building_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp2_normal1",
		idle = 0,
		prior = 7, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_nedostroyka_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp2_alarm1",
		idle = 0,
		prior = 7, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_nedostroyka_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp2_alarm1",
		idle = 0,
		prior = 7, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_nedostroyka_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp2_normal2",
		idle = 0,
		prior = 7, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_nedostroyka_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp2_alarm2",
		idle = 0,
		prior = 7, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_nedostroyka_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp2_alarm2",
		idle = 0,
		prior = 7, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_nedostroyka_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp2_normal3",
		idle = 0,
		prior = 7, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_nedostroyka_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp2_alarm3",
		idle = 0,
		prior = 7, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_nedostroyka_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp2_alarm3",
		idle = 0,
		prior = 7, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_nedostroyka_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp2_normal4",
		idle = 0,
		prior = 7, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_nedostroyka_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp2_alarm4",
		idle = 0,
		prior = 7, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_nedostroyka_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp2_alarm4",
		idle = 0,
		prior = 7, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_nedostroyka_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)


	-----------------------------------------------------------------------
	-- œ–»Œ–»“≈“ 6 - ¡¿Õƒ»“€ Õ¿ ¬Œ–Œ“¿’
	-----------------------------------------------------------------------
	t = { section = "logic@val_lager_bandits_guard_normal1",
		idle = 0,
		prior = 6, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_main_side_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_guard_alarm1",
		idle = 0,
		prior = 6, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_main_side_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_guard_alarm1",
		idle = 0,
		prior = 6, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_main_side_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_guard_normal2",
		idle = 0,
		prior = 6, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_guard_alarm2",
		idle = 0,
		prior = 6, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_guard_alarm2",
		idle = 0,
		prior = 6, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	-----------------------------------------------------------------------
	-- œ–»Œ–»“≈“ 5 - œ¿“–”À‹Õ€≈ Õ¿ “≈––»“Œ–»»
	-----------------------------------------------------------------------
	t = { section = "logic@val_lager_bandits_patrol_normal1",
		idle = 0,
		prior = 5, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_main_side_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_patrol_alarm1",
		idle = 0,
		prior = 5, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_main_side_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_patrol_alarm1",
		idle = 0,
		prior = 5, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_main_side_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_patrol_normal2",
		idle = 0,
		prior = 5, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_patrol_alarm2",
		idle = 0,
		prior = 5, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_patrol_alarm2",
		idle = 0,
		prior = 5, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_patrol_normal3",
		idle = 0,
		prior = 5, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_patrol_alarm3",
		idle = 0,
		prior = 5, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_patrol_alarm3",
		idle = 0,
		prior = 5, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_patrol_normal4",
		idle = 0,
		prior = 5, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_patrol_alarm4",
		idle = 0,
		prior = 5, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_patrol_alarm4",
		idle = 0,
		prior = 5, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	-----------------------------------------------------------------------
	-- œ–»Œ–»“≈“ 4 - —Õ¿…œ≈–€ Õ¿ ¬€ÿ ¿’
	-----------------------------------------------------------------------
	t = { section = "logic@val_lager_bandits_sniper_normal1",
		idle = 0,
		prior = 4, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_sniper_alarm1",
		idle = 0,
		prior = 4, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_sniper_alarm1",
		idle = 0,
		prior = 4, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_sniper_normal2",
		idle = 0,
		prior = 4, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_sniper_alarm2",
		idle = 0,
		prior = 4, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_sniper_alarm2",
		idle = 0,
		prior = 4, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	-----------------------------------------------------------------------
	-- œ–»Œ–»“≈“ 3 - —œﬂŸ»≈ ¡¿Õƒ»“€
	-----------------------------------------------------------------------

	t = { section = "logic@val_lager_bandits_sleeper_normal1",
		idle = 0,
		prior = 3, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_sleeper_alarm1",
		idle = 0,
		prior = 3, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_sleeper_alarm1",
		idle = 0,
		prior = 3, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_sleeper_normal2",
		idle = 0,
		prior = 3, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_sleeper_alarm2",
		idle = 0,
		prior = 3, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_sleeper_alarm2",
		idle = 0,
		prior = 3, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_sleeper_normal3",
		idle = 0,
		prior = 3, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_sleeper_alarm3",
		idle = 0,
		prior = 3, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_sleeper_alarm3",
		idle = 0,
		prior = 3, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_sleeper_normal4",
		idle = 0,
		prior = 3, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_sleeper_alarm4",
		idle = 0,
		prior = 3, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_sleeper_alarm4",
		idle = 0,
		prior = 3, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	-----------------------------------------------------------------------
	-- œ–»Œ–»“≈“ 2 - ¡¿Õƒ»“€ ¬ 3-Ã  ≈Ãœ≈
	-----------------------------------------------------------------------

	t = { section = "logic@val_lager_bandits_kamp3_normal1",
		idle = 0,
		prior = 2, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_nedostroyka_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp3_alarm1",
		idle = 0,
		prior = 2, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_nedostroyka_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp3_alarm1",
		idle = 0,
		prior = 2, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_nedostroyka_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp3_normal2",
		idle = 0,
		prior = 2, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_nedostroyka_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp3_alarm2",
		idle = 0,
		prior = 2, state = { state_alarm },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_nedostroyka_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_lager_bandits_kamp3_alarm2",
		idle = 0,
		prior = 2, state = { state_alarm_actor_inside },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_lager_bandits_nedostroyka_restrictor",
		predicate = lb_predicate3
		            --function(obj) 
		            --	return obj:profile_name() == "bandit_general"
		            --end
	}
	table.insert(sj, t)

end

function mb_predicate(obj)
    local pname = obj:profile_name()
	return pname == "monolit_regular"
end

function load_job_monolith_base(sj, gname, type, squad, groups)
	-----------------------------------------------------------------------
	-- œ–»Œ–»“≈“ 4 - œ¿“–”À» Õ¿ “≈––»“Œ–»»
	-----------------------------------------------------------------------
	t = { section = "logic@val_monolith_base_patrol_normal1",
		idle = 0,
		prior = 4, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_monolith_base_patrol_normal2",
		idle = 0,
		prior = 4, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)
	
	t = { section = "logic@val_monolith_base_patrol_normal3",
		idle = 0,
		prior = 4, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_monolith_base_patrol_normal4",
		idle = 0,
		prior = 4, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_monolith_base_patrol_normal5",
		idle = 0,
		prior = 4, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_monolith_base_patrol_normal6",
		idle = 0,
		prior = 4, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	-----------------------------------------------------------------------
	-- œ–»Œ–»“≈“ 1 - œ≈–¬€…  ≈Ãœ
	-----------------------------------------------------------------------
	t = { section = "logic@val_monolith_base_kamp1_normal1",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_monolith_base_kamp1_normal2",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_monolith_base_kamp1_normal3",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_monolith_base_kamp1_normal4",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	-----------------------------------------------------------------------
	-- œ–»Œ–»“≈“ 1 - ¬“Œ–Œ…  ≈Ãœ
	-----------------------------------------------------------------------
	t = { section = "logic@val_monolith_base_kamp2_normal1",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_monolith_base_kamp2_normal2",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_monolith_base_kamp2_normal3",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_monolith_base_kamp2_normal4",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	-----------------------------------------------------------------------
	-- œ–»Œ–»“≈“ 1 - “–≈“»…  ≈Ãœ
	-----------------------------------------------------------------------
	t = { section = "logic@val_monolith_base_kamp3_normal1",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_monolith_base_kamp3_normal2",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_monolith_base_kamp3_normal3",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_monolith_base_kamp3_normal4",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	-----------------------------------------------------------------------
	-- œ–»Œ–»“≈“ 3 - ◊≈“¬≈–“€…  ≈Ãœ
	-----------------------------------------------------------------------
	t = { section = "logic@val_monolith_base_kamp4_normal1",
		idle = 0,
		prior = 3, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_monolith_base_kamp4_normal2",
		idle = 0,
		prior = 3, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_monolith_base_kamp4_normal3",
		idle = 0,
		prior = 3, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_monolith_base_kamp4_normal4",
		idle = 0,
		prior = 3, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_monolith_base_kamp4_normal5",
		idle = 0,
		prior = 3, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	-----------------------------------------------------------------------
	-- œ–»Œ–»“≈“ 2 - œﬂ“€…  ≈Ãœ
	-----------------------------------------------------------------------
	t = { section = "logic@val_monolith_base_kamp5_normal1",
		idle = 0,
		prior = 2, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_monolith_base_kamp5_normal2",
		idle = 0,
		prior = 2, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj) 
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_monolith_base_kamp5_normal3",
		idle = 0,
		prior = 2, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj)
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)

	t = { section = "logic@val_monolith_base_kamp5_normal4",
		idle = 0,
		prior = 2, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_main_building_restrictor",
		predicate = mb_predicate
		            --function(obj)
		            --	return obj:profile_name() == "monolit_regular"
		            --end
	}
	table.insert(sj, t)
end

function ws_predicate(obj)
    local pname = obj:profile_name()
	return pname == "soldier_regular" or pname == "soldier_specnaz"
end

function load_job_war_soldiers(sj, gname, type, squad, groups)
	-----------------------------------------------------------------------
	-- œ–»Œ–»“≈“ 4 - œ¿“–”À» Õ¿ “≈––»“Œ–»»
	-----------------------------------------------------------------------
	t = { section = "logic@val_war_soldiers_patrol_normal1",
		idle = 0,
		prior = 4, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight1",
		idle = 0,
		prior = 4, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_patrol_normal2",
		idle = 0,
		prior = 4, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)
	
	t = { section = "logic@val_war_soldiers_fight2",
		idle = 0,
		prior = 4, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_patrol_normal3",
		idle = 0,
		prior = 4, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight3",
		idle = 0,
		prior = 4, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_patrol_normal4",
		idle = 0,
		prior = 4, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight4",
		idle = 0,
		prior = 4, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_patrol_normal5",
		idle = 0,
		prior = 4, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight5",
		idle = 0,
		prior = 4, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_patrol_normal6",
		idle = 0,
		prior = 4, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight6",
		idle = 0,
		prior = 4, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	-----------------------------------------------------------------------
	-- œ–»Œ–»“≈“ 1 - œ≈–¬€…  ≈Ãœ
	-----------------------------------------------------------------------
	t = { section = "logic@val_war_soldiers_kamp1_normal1",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight7",
		idle = 0,
		prior = 1, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_kamp1_normal2",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight8",
		idle = 0,
		prior = 1, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_kamp1_normal3",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight9",
		idle = 0,
		prior = 1, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_kamp1_normal4",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight10",
		idle = 0,
		prior = 1, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	-----------------------------------------------------------------------
	-- œ–»Œ–»“≈“ 1 - ¬“Œ–Œ…  ≈Ãœ
	-----------------------------------------------------------------------
	t = { section = "logic@val_war_soldiers_kamp2_normal1",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight11",
		idle = 0,
		prior = 1, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_kamp2_normal2",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight12",
		idle = 0,
		prior = 1, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_kamp2_normal3",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight13",
		idle = 0,
		prior = 1, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_kamp2_normal4",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight14",
		idle = 0,
		prior = 1, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	-----------------------------------------------------------------------
	-- œ–»Œ–»“≈“ 1 - “–≈“»…  ≈Ãœ
	-----------------------------------------------------------------------
	t = { section = "logic@val_war_soldiers_kamp3_normal1",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight15",
		idle = 0,
		prior = 1, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_kamp3_normal2",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight16",
		idle = 0,
		prior = 1, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_kamp3_normal3",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight17",
		idle = 0,
		prior = 1, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_kamp3_normal4",
		idle = 0,
		prior = 1, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight18",
		idle = 0,
		prior = 1, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	-----------------------------------------------------------------------
	-- œ–»Œ–»“≈“ 3 - ◊≈“¬≈–“€…  ≈Ãœ
	-----------------------------------------------------------------------
	t = { section = "logic@val_war_soldiers_kamp4_normal1",
		idle = 0,
		prior = 3, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight19",
		idle = 0,
		prior = 1, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_kamp4_normal2",
		idle = 0,
		prior = 3, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight20",
		idle = 0,
		prior = 1, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_kamp4_normal3",
		idle = 0,
		prior = 3, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight21",
		idle = 0,
		prior = 1, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_kamp4_normal4",
		idle = 0,
		prior = 3, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight22",
		idle = 0,
		prior = 1, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_kamp4_normal5",
		idle = 0,
		prior = 3, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight23",
		idle = 0,
		prior = 1, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	-----------------------------------------------------------------------
	-- œ–»Œ–»“≈“ 2 - œﬂ“€…  ≈Ãœ
	-----------------------------------------------------------------------
	t = { section = "logic@val_war_soldiers_kamp5_normal1",
		idle = 0,
		prior = 2, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight24",
		idle = 0,
		prior = 1, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_kamp5_normal2",
		idle = 0,
		prior = 2, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight25",
		idle = 0,
		prior = 1, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_kamp5_normal3",
		idle = 0,
		prior = 2, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight26",
		idle = 0,
		prior = 1, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_kamp5_normal4",
		idle = 0,
		prior = 2, state = { state_normal },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_war_soldiers_main_building_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)

	t = { section = "logic@val_war_soldiers_fight27",
		idle = 0,
		prior = 1, state = { state_fight },
		squad = squad, group = groups[1],
		in_rest = "", out_rest = "val_monolith_base_war_restrictor",
		predicate = ws_predicate
	}
	table.insert(sj, t)
end

--- Dogs
function load_job_dogs_nest1(sj, gname, type, squad, groups)
    local i = 0
    while i < 8 do
        --- state 0
	    t = { section = "logic@val_dogs_nest1", 
		    idle = 0,
		    prior = 5, state = {0}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_dogs_nest1_small_restr"
	    }
	    table.insert(sj, t)
        --- state 1
	    t = { section = "logic@val_dogs_nest1", 
		    idle = 0,
		    prior = 5, state = {1}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_dogs_nest1_big_restr"
	    }
        table.insert(sj, t)
        i = i + 1
   end
end

function load_job_dogs_nest2(sj, gname, type, squad, groups)
    local i = 0
    while i < 10 do
        --- state 0
	    t = { section = "logic@val_dogs_nest2", 
		    idle = 0,
		    prior = 5, state = {0}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_dogs_nest2_small_restr"
	    }
	    table.insert(sj, t)
        --- state 1
	    t = { section = "logic@val_dogs_nest2", 
		    idle = 0,
		    prior = 5, state = {1}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_dogs_nest2_big_restr"
	    }
        table.insert(sj, t)
        i = i + 1
   end
end

function load_job_dogs_nest3(sj, gname, type, squad, groups)
    local i = 0
    while i < 8 do
        --- state 0
	    t = { section = "logic@val_dogs_nest3", 
		    idle = 0,
		    prior = 5, state = {0}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_dogs_nest3_small_restr"
	    }
	    table.insert(sj, t)
        --- state 1
	    t = { section = "logic@val_dogs_nest3", 
		    idle = 0,
		    prior = 5, state = {1}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_dogs_nest3_big_restr"
	    }
        table.insert(sj, t)
        i = i + 1
   end
end

function load_job_dogs_nest4(sj, gname, type, squad, groups)
    local i = 0
    while i < 8 do
        --- state 0
	    t = { section = "logic@val_dogs_nest4", 
		    idle = 0,
		    prior = 5, state = {0}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_dogs_nest4_small_restr"
	    }
	    table.insert(sj, t)
        --- state 1
	    t = { section = "logic@val_dogs_nest4", 
		    idle = 0,
		    prior = 5, state = {1}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_dogs_nest4_big_restr"
	    }
        table.insert(sj, t)
        i = i + 1
   end
end

function load_job_dogs_nest5(sj, gname, type, squad, groups)
    local i = 0
    while i < 10 do
        --- state 0
	    t = { section = "logic@val_dogs_nest2", 
		    idle = 0,
		    prior = 5, state = {0}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_dogs_nest5_small_restr"
	    }
	    table.insert(sj, t)
        --- state 1
	    t = { section = "logic@val_dogs_nest5", 
		    idle = 0,
		    prior = 5, state = {1}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_dogs_nest5_big_restr"
	    }
        table.insert(sj, t)
        i = i + 1
   end
end

--- Fleshes
function load_job_fleshes_nest1(sj, gname, type, squad, groups)
    local i = 0
    while i < 6 do
        --- state 0
	    t = { section = "logic@val_fleshes_nest1", 
		    idle = 0,
		    prior = 5, state = {0}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_fleshes_nest1_small_restr"
	    }
	    table.insert(sj, t)
        --- state 1
	    t = { section = "logic@val_fleshes_nest1", 
		    idle = 0,
		    prior = 5, state = {1}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_fleshes_nest1_big_restr"
	    }
        table.insert(sj, t)
        i = i + 1
   end
end

function load_job_fleshes_nest2(sj, gname, type, squad, groups)
    local i = 0
    while i < 6 do
        --- state 0
	    t = { section = "logic@val_fleshes_nest2", 
		    idle = 0,
		    prior = 5, state = {0}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_fleshes_nest2_small_restr"
	    }
	    table.insert(sj, t)
        --- state 1
	    t = { section = "logic@val_fleshes_nest2", 
		    idle = 0,
		    prior = 5, state = {1}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_fleshes_nest2_big_restr"
	    }
        table.insert(sj, t)
        i = i + 1
   end
end

function load_job_fleshes_nest3(sj, gname, type, squad, groups)
    local i = 0
    while i < 4 do
        --- state 0
	    t = { section = "logic@val_fleshes_nest3", 
		    idle = 0,
		    prior = 5, state = {0}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_fleshes_nest3_small_restr"
	    }
	    table.insert(sj, t)
        --- state 1
	    t = { section = "logic@val_fleshes_nest3", 
		    idle = 0,
		    prior = 5, state = {1}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_fleshes_nest3_big_restr"
	    }
        table.insert(sj, t)
        i = i + 1
   end
end

function load_job_fleshes_nest4(sj, gname, type, squad, groups)
    local i = 0
    while i < 4 do
        --- state 0
	    t = { section = "logic@val_fleshes_nest4", 
		    idle = 0,
		    prior = 5, state = {0}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_fleshes_nest4_small_restr"
	    }
	    table.insert(sj, t)
        --- state 1
	    t = { section = "logic@val_fleshes_nest4", 
		    idle = 0,
		    prior = 5, state = {1}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_fleshes_nest4_big_restr"
	    }
        table.insert(sj, t)
        i = i + 1
   end
end

function load_job_fleshes_nest5(sj, gname, type, squad, groups)
    local i = 0
    while i < 4 do
        --- state 0
	    t = { section = "logic@val_fleshes_nest5", 
		    idle = 0,
		    prior = 5, state = {0}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_fleshes_nest5_small_restr"
	    }
	    table.insert(sj, t)
        --- state 1
	    t = { section = "logic@val_fleshes_nest5", 
		    idle = 0,
		    prior = 5, state = {1}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_fleshes_nest5_big_restr"
	    }
        table.insert(sj, t)
        i = i + 1
   end
end

--- Boars
function load_job_boars_nest1(sj, gname, type, squad, groups)
    local i = 0
    while i < 3 do
        --- state 0
	    t = { section = "logic@val_boars_nest1", 
		    idle = 0,
		    prior = 5, state = {0}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_boars_nest1_small_restr"
	    }
	    table.insert(sj, t)
        --- state 1
	    t = { section = "logic@val_boars_nest1", 
		    idle = 0,
		    prior = 5, state = {1}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_boars_nest1_big_restr"
	    }
        table.insert(sj, t)
        i = i + 1
   end
end

function load_job_boars_nest2(sj, gname, type, squad, groups)
    local i = 0
    while i < 5 do
        --- state 0
	    t = { section = "logic@val_boars_nest2", 
		    idle = 0,
		    prior = 5, state = {0}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_boars_nest2_small_restr"
	    }
	    table.insert(sj, t)
        --- state 1
	    t = { section = "logic@val_boars_nest2", 
		    idle = 0,
		    prior = 5, state = {1}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_boars_nest2_big_restr"
	    }
        table.insert(sj, t)
        i = i + 1
   end
end

function load_job_boars_nest3(sj, gname, type, squad, groups)
    local i = 0
    while i < 5 do
        --- state 0
	    t = { section = "logic@val_boars_nest3", 
		    idle = 0,
		    prior = 5, state = {0}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_boars_nest3_small_restr"
	    }
	    table.insert(sj, t)
        --- state 1
	    t = { section = "logic@val_boars_nest3", 
		    idle = 0,
		    prior = 5, state = {1}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_boars_nest3_big_restr"
	    }
        table.insert(sj, t)
        i = i + 1
   end
end

function load_job_boars_nest4(sj, gname, type, squad, groups)
    local i = 0
    while i < 5 do
        --- state 0
	    t = { section = "logic@val_boars_nest4", 
		    idle = 0,
		    prior = 5, state = {0}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_boars_nest4_small_restr"
	    }
	    table.insert(sj, t)
        --- state 1
	    t = { section = "logic@val_boars_nest4", 
		    idle = 0,
		    prior = 5, state = {1}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_boars_nest4_big_restr"
	    }
        table.insert(sj, t)
        i = i + 1
   end
end

function load_job_boars_nest5(sj, gname, type, squad, groups)
    local i = 0
    while i < 5 do
        --- state 0
	    t = { section = "logic@val_boars_nest5", 
		    idle = 0,
		    prior = 5, state = {0}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_boars_nest5_small_restr"
	    }
	    table.insert(sj, t)
        --- state 1
	    t = { section = "logic@val_boars_nest5", 
		    idle = 0,
		    prior = 5, state = {1}, squad = squad, group = groups[1],
		    in_rest = "", out_rest = "val_boars_nest5_big_restr"
	    }
        table.insert(sj, t)
        i = i + 1
   end
end

--------------------------------------------------------------------------
function load_states(gname, type)
    --- Soldiers -------------------------------------------
	if type == "val_lager_bandits"      then    return check_state_lager_bandits
	elseif type == "val_monolith_base"  then    return check_state_monolith_base
	elseif type == "val_war_soldiers"   then    return check_state_war_soldiers
	--- Monsters -------------------------------------------
	--- Dogs
	elseif type == "val_dogs_nest1"     then    return check_state_dogs_nest1
	elseif type == "val_dogs_nest2"     then    return check_state_dogs_nest2
	elseif type == "val_dogs_nest3"     then    return check_state_dogs_nest3
	elseif type == "val_dogs_nest4"     then    return check_state_dogs_nest4
	elseif type == "val_dogs_nest5"     then    return check_state_dogs_nest5
	--- Fleshes
	elseif type == "val_fleshes_nest1"  then    return check_state_fleshes_nest1
	elseif type == "val_fleshes_nest2"  then    return check_state_fleshes_nest2
	elseif type == "val_fleshes_nest3"  then    return check_state_fleshes_nest3
	elseif type == "val_fleshes_nest4"  then    return check_state_fleshes_nest4
	elseif type == "val_fleshes_nest5"  then    return check_state_fleshes_nest5
	--- Boars
	elseif type == "val_boars_nest1"    then    return check_state_boars_nest1
	elseif type == "val_boars_nest2"    then    return check_state_boars_nest2
	elseif type == "val_boars_nest3"    then    return check_state_boars_nest3
	elseif type == "val_boars_nest4"    then    return check_state_boars_nest4
	elseif type == "val_boars_nest5"    then    return check_state_boars_nest5
	end

	return nil
end

function check_state_lager_bandits(gulag)
	for k,v in gulag.Object do
		local enemy = v:best_enemy()
		if enemy ~= nil then
			enemy_type = get_clsid(enemy)
			if enemy_type == clsid.actor and
				utils.npc_in_zone(db.actor, db.zone_by_name["val_lager_bandits_restrictor"]) then
				return state_alarm_actor_inside
			end
			if enemy_type == clsid.actor or
  	   			enemy_type == clsid.soldier or
	   			enemy_type == clsid.script_stalker or
   				enemy_type == clsid.trader
   			then
   				gulag.alarm_start = game.get_game_time()
   				return state_alarm
   			end
		end
	end		
	
	if gulag.alarm_start == nil or game.get_game_time():diffSec(gulag.alarm_start) > 900 then
		return state_normal -- Ó·˚˜ÌÓÂ ÒÓÒÚÓˇÌËÂ
	end
	return state_alarm -- ÚÂ‚Ó„‡
end

function check_state_monolith_base(gulag)
	return state_normal
end

function check_state_war_soldiers(gulag)
	if db.actor:has_info("val_opendoor_chase_start") then
		return state_normal
	end
	return state_fight
end

function check_state_dogs_nest1(gulag)
	local t = db.zone_by_name["val_dogs_nest1_big_restr"] -- ËÏˇ ·ÓÎ¸¯Ó„Ó ÂÒÚ.
	if t and db.actor then
		if t:inside(db.actor:position()) then
			return 1
		else
			return 0
		end
	else
		return gulag.state
	end
end

function check_state_dogs_nest2(gulag)
	local t = db.zone_by_name["val_dogs_nest2_big_restr"] -- ËÏˇ ·ÓÎ¸¯Ó„Ó ÂÒÚ.
	if t and db.actor then
		if t:inside(db.actor:position()) then
			return 1
		else
			return 0
		end
	else
		return gulag.state
	end
end

function check_state_dogs_nest3(gulag)
	local t = db.zone_by_name["val_dogs_nest3_big_restr"] -- ËÏˇ ·ÓÎ¸¯Ó„Ó ÂÒÚ.
	if t and db.actor then
		if t:inside(db.actor:position()) then
			return 1
		else
			return 0
		end
	else
		return gulag.state
	end
end

function check_state_dogs_nest4(gulag)
	local t = db.zone_by_name["val_dogs_nest4_big_restr"] -- ËÏˇ ·ÓÎ¸¯Ó„Ó ÂÒÚ.
	if t and db.actor then
		if t:inside(db.actor:position()) then
			return 1
		else
			return 0
		end
	else
		return gulag.state
	end
end

function check_state_dogs_nest5(gulag)
	local t = db.zone_by_name["val_dogs_nest5_big_restr"] -- ËÏˇ ·ÓÎ¸¯Ó„Ó ÂÒÚ.
	if t and db.actor then
		if t:inside(db.actor:position()) then
			return 1
		else
			return 0
		end
	else
		return gulag.state
	end
end

function check_state_fleshes_nest1(gulag)
	local t = db.zone_by_name["val_fleshes_nest1_big_restr"] -- ËÏˇ ·ÓÎ¸¯Ó„Ó ÂÒÚ.
	if t and db.actor then
		if t:inside(db.actor:position()) then
			return 1
		else
			return 0
		end
	else
		return gulag.state
	end
end

function check_state_fleshes_nest2(gulag)
	local t = db.zone_by_name["val_fleshes_nest2_big_restr"] -- ËÏˇ ·ÓÎ¸¯Ó„Ó ÂÒÚ.
	if t and db.actor then
		if t:inside(db.actor:position()) then
			return 1
		else
			return 0
		end
	else
		return gulag.state
	end
end

function check_state_fleshes_nest3(gulag)
	local t = db.zone_by_name["val_fleshes_nest3_big_restr"] -- ËÏˇ ·ÓÎ¸¯Ó„Ó ÂÒÚ.
	if t and db.actor then
		if t:inside(db.actor:position()) then
			return 1
		else
			return 0
		end
	else
		return gulag.state
	end
end

function check_state_fleshes_nest4(gulag)
	local t = db.zone_by_name["val_fleshes_nest4_big_restr"] -- ËÏˇ ·ÓÎ¸¯Ó„Ó ÂÒÚ.
	if t and db.actor then
		if t:inside(db.actor:position()) then
			return 1
		else
			return 0
		end
	else
		return gulag.state
	end
end

function check_state_fleshes_nest5(gulag)
	local t = db.zone_by_name["val_fleshes_nest5_big_restr"] -- ËÏˇ ·ÓÎ¸¯Ó„Ó ÂÒÚ.
	if t and db.actor then
		if t:inside(db.actor:position()) then
			return 1
		else
			return 0
		end
	else
		return gulag.state
	end
end

function check_state_boars_nest1(gulag)
	local t = db.zone_by_name["val_boars_nest1_big_restr"] -- ËÏˇ ·ÓÎ¸¯Ó„Ó ÂÒÚ.
	if t and db.actor then
		if t:inside(db.actor:position()) then
			return 1
		else
			return 0
		end
	else
		return gulag.state
	end
end

function check_state_boars_nest2(gulag)
	local t = db.zone_by_name["val_boars_nest2_big_restr"] -- ËÏˇ ·ÓÎ¸¯Ó„Ó ÂÒÚ.
	if t and db.actor then
		if t:inside(db.actor:position()) then
			return 1
		else
			return 0
		end
	else
		return gulag.state
	end
end

function check_state_boars_nest3(gulag)
	local t = db.zone_by_name["val_boars_nest3_big_restr"] -- ËÏˇ ·ÓÎ¸¯Ó„Ó ÂÒÚ.
	if t and db.actor then
		if t:inside(db.actor:position()) then
			return 1
		else
			return 0
		end
	else
		return gulag.state
	end
end

function check_state_boars_nest4(gulag)
	local t = db.zone_by_name["val_boars_nest4_big_restr"] -- ËÏˇ ·ÓÎ¸¯Ó„Ó ÂÒÚ.
	if t and db.actor then
		if t:inside(db.actor:position()) then
			return 1
		else
			return 0
		end
	else
		return gulag.state
	end
end

function check_state_boars_nest5(gulag)
	local t = db.zone_by_name["val_boars_nest5_big_restr"] -- ËÏˇ ·ÓÎ¸¯Ó„Ó ÂÒÚ.
	if t and db.actor then
		if t:inside(db.actor:position()) then
			return 1
		else
			return 0
		end
	else
		return gulag.state
	end
end

--------------------------------------------------------------------------------------
function checkStalker(npc_community, gulag_type)
	if gulag_type == "val_lager_bandits"        then    return npc_community == "bandit"
	elseif gulag_type == "val_monolith_base"    then    return npc_community == "monolith"
	elseif gulag_type == "val_war_soldiers"     then    return npc_community == "military"
	end    

	return false
end

function checkMonster(npc_community, gulag_type)
    if      gulag_type == "val_dogs_nest1" or
            gulag_type == "val_dogs_nest2" or
            gulag_type == "val_dogs_nest3" or
            gulag_type == "val_dogs_nest4" or
            gulag_type == "val_dogs_nest5"      then return npc_community == "dog"

    elseif  gulag_type == "val_fleshes_nest1" or
            gulag_type == "val_fleshes_nest2" or
            gulag_type == "val_fleshes_nest3" or
            gulag_type == "val_fleshes_nest4" or
            gulag_type == "val_fleshes_nest5"   then return npc_community == "flesh"

    elseif  gulag_type == "val_boars_nest1" or
            gulag_type == "val_boars_nest2" or
            gulag_type == "val_boars_nest3" or
            gulag_type == "val_boars_nest4" or
            gulag_type == "val_boars_nest5"     then return npc_community == "boar"
    end

    return false
end
