module("ui_save_dialog")
module("ui_load_dialog")

function dump_time(t)
/*	printf(t:dateToString(game.CTime.DateToDay))
	printf(t:dateToString(game.CTime.DateToMonth))
	printf(t:dateToString(game.CTime.DateToYear))
	
	printf(t:timeToString(game.CTime.TimeToHours))
	printf(t:timeToString(game.CTime.TimeToMinutes))
	printf(t:timeToString(game.CTime.TimeToSeconds))
*/	
	printf(t:timeToString(game.CTime.TimeToMilisecs))
printf("")
end

function main()
printf("----------------------main----")
	local dt = game.CTime()
	dt:setHMSms(3,4,5,67)
	dump_time(dt)
	
	local dt2 = game.CTime(dt)
	dump_time(dt2)
	dump_time(dt)
	
	if dt==dt2 then 
		printf("equal")
	else
		printf("not equal")
	end

	dt2:setHMSms(13,4,5,67)
	dump_time(dt2)
	dump_time(dt) 

	if dt==dt2 then 
		printf("equal")
	else
		printf("not equal")
	end

	
end
