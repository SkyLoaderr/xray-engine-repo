

function main______________()
	local spawn_items = {}
	local level = level.name()

	table.insert(spawn_items, "wpn_ak74")
	table.insert(spawn_items, "wpn_addon_scope")
	table.insert(spawn_items, "wpn_addon_silencer")
	table.insert(spawn_items, "wpn_addon_grenade_launcher")
	
	table.insert(spawn_items, "device_torch")
	

	local k,v = 0,0
	for k,v in pairs(spawn_items) do
		alife():create(v,	
						db.actor:position(),
						db.actor:level_vertex_id(),	
						db.actor:game_vertex_id())
	end	
end

function compl1()
	return false
end
function compl2()
	return false
end
function fail1()
	return false
end

function main()

	  db.actor:give_info_portion("test_andy_info") 

      local task = CGameTask()
        
        task:load("eliminate_lager")
        task:set_title("title_0")
        
        local objective = SGameTaskObjective(task,1)
        objective:set_description("test_description_1")
		objective:add_complete_func("_test.compl1")
		objective:add_complete_func("_test.compl2")
		objective:add_fail_func("_test.fail1")
        task:add_objective(objective)

        db.actor:give_task(task,false)
  
--   game.start_tutorial("part_1_pda")
end

function main1()
	local info = "test_andy_info" 
--	local info = "tutorial_artefact_start"
	db.actor:give_info_portion("descr_bar_resque_information") 
end

function main2()

local _task = CGameTask()
_task:load("andy_task_2")
_task:set_title("my_new_title_from_script")

local _obj = SGameTaskObjective(_task,1)
_obj:set_description("descr_from_script_for_1")
_obj:set_article_id("descr_from_script_for_1")

--map locations
-- _obj:set_map_hint("xxx")
-- _obj:set_map_location("xxx")
-- _obj:set_object_id(0)

-- _obj:set_article_key("descr_from_script_for_0")

_obj:add_complete_info("descr_from_script_for_1")
_obj:add_fail_info("descr_from_script_for_1")
_obj:add_on_complete_info("descr_from_script_for_1")
_obj:add_on_fail_info("descr_from_script_for_1")

_task:add_objective(_obj)

_obj = SGameTaskObjective(_task,2)
_obj:set_description("descr_from_script_for_2")
_obj:set_article_id("descr_from_script_for_2")

--map locations
-- _obj:set_map_hint("xxx")
-- _obj:set_map_location("xxx")
-- _obj:set_object_id(0)

-- _obj:set_article_key("descr_from_script_for_0")

_obj:add_complete_info("descr_from_script_for_2")
_obj:add_fail_info("descr_from_script_for_2")
_obj:add_on_complete_info("descr_from_script_for_2")
_obj:add_on_fail_info("descr_from_script_for_2")

_task:add_objective(_obj)


db.actor:give_task(_task,false)

--	db.actor:actor_sleep(0,180)
	
	
--	info = "tutorial_artefact_start" 
--	db.actor:give_info_portion(info) 
	
	
--	info = "tutorial_wounded_start" 
--	db.actor:give_info_portion(info) 


--	level.add_cam_effector("camera_effects\\prison_1.anm", 999, true)
--	level.add_cam_effector("camera_effects\\prison_1.anm", 999, true)
--	level.remove_cam_effector(999)
--	level.set_pp_effector_factor(999, 0.1)
--	level.remove_pp_effector(999)
end

function main________()
	db.actor:give_game_news("hello-1 sdfewrf ", "ui\\ui_icons_task", Frect():set(300,100,50,50),0,0)

--[[
	db.actor:give_game_news("hello-1", "ui\\ui_icons_task", Frect():set(300,100,50,50),0,0)
	db.actor:give_game_news("hello-2", "ui\\ui_icons_task", Frect():set(300,100,50,50),0,0)
	db.actor:give_game_news("hello-3", "ui\\ui_icons_task", Frect():set(300,100,50,50),0,0)
	db.actor:give_game_news("hello-4", "ui\\ui_icons_task", Frect():set(300,100,50,50),0,0)
	db.actor:give_game_news("hello-5", "ui\\ui_icons_task", Frect():set(300,100,50,50),0,0)
	db.actor:give_game_news("hello-6", "ui\\ui_icons_task", Frect():set(300,100,50,50),0,0)
	db.actor:give_game_news("hello-7", "ui\\ui_icons_task", Frect():set(300,100,50,50),0,0)
	db.actor:give_game_news("hello-8", "ui\\ui_icons_task", Frect():set(300,100,50,50),0,0)
	db.actor:give_game_news("hello-9", "ui\\ui_icons_task", Frect():set(300,100,50,50),0,0)

	db.actor:give_game_news("general_fail_task", "ui\\ui_icons_task", Frect():set(300,100,50,50),0,0)
	db.actor:give_game_news("general_complete_task", "ui\\ui_icons_task", Frect():set(350,100,50,50), 0, 5000)
]]	
end

function main______()
   local h = hit ()
   h.power = 600
   h.direction = vector():set(0, 0, 1)
   h.impulse = 100
   h.draftsman = db.actor
   h.type = hit.radiation
   db.actor:hit(h)
   
   h.power = 600
   h.direction = vector():set(0, 0, 1)
   h.impulse = 100
   h.draftsman = db.actor
   h.type = hit.wound
   db.actor:hit(h)

   h.power = 600
   h.direction = vector():set(0, 0, 1)
   h.impulse = 100
   h.draftsman = db.actor
   h.type = hit.telepatic
  db.actor:hit(h)

   h.power = 600
   h.direction = vector():set(0, 0, 1)
   h.impulse = 100
   h.draftsman = db.actor
   h.type = hit.strike
  db.actor:hit(h)

   h.power = 600
   h.direction = vector():set(0, 0, 1)
   h.impulse = 100
   h.draftsman = db.actor
   h.type = hit.chemical_burn
  db.actor:hit(h)

   h.power = 600
   h.direction = vector():set(0, 0, 1)
   h.impulse = 100
   h.draftsman = db.actor
   h.type = hit.shock
  db.actor:hit(h)
  
   h.power = 600
   h.direction = vector():set(0, 0, 1)
   h.impulse = 100
   h.draftsman = db.actor
   h.type = hit.burn
  db.actor:hit(h)

h.power = 600
   h.direction = vector():set(0, 0, 1)
   h.impulse = 100
   h.draftsman = db.actor
   h.type = hit.explosion
  db.actor:hit(h)
  
--burn  1
--shock  1
--strike  1
--wound  1
--radiation   1
--telepatic   1
--chemical_burn 1
--explosion

end

function _main()
--	"stalkerkills"
--	"monsterkills"
--	"quests"
--	"artefacts"

--actor_stats.add_points("stalkerkills","dolg",2,22)
--actor_stats.add_points("stalkerkills","svoboda",1,45)
--actor_stats.add_points("stalkerkills","urod",1,5)
--actor_stats.add_points("artefacts","af_1",1,800)
--actor_stats.add_points("quests","help wounded",1,2000)

--	level.add_cam_effector("camera_effects\\prison_1.anm")	

--   local h = hit ()
--   h.power = 100
--   h.direction = vector():set(0, 0, 1)
--   h.impulse = 100
--   h.draftsman = db.actor
--   h.type = hit.fire_wound
--   db.actor:hit(h)
	
--	local info = "esc_kill_gunslinger" 
--	db.actor:give_info_portion(info) 

   
end
